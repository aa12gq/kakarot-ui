import createVueComponent from '../createVueComponent';

const TodayRounded = createVueComponent('TodayRoundedIcon', [
  ['g', { id: 'today_rounded', key: 'us3ntu' }],
]);

export default TodayRounded;
